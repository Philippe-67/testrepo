@model IEnumerable<WebApplication1.Models.Vocabulaire>

@{
    ViewData["Title"] = "Index";
}

<h1>Le trésor</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.MotFrancais)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TraductionAnglais)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.MotFrancais)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TraductionAnglais)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
